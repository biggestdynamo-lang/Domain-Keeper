{
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm run typecheck:libs && BASE_PATH=/ NODE_ENV=production pnpm --filter @workspace/freeable-domains run build",
  "outputDirectory": "artifacts/freeable-domains/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
